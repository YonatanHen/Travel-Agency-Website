apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: travel-agency
  labels:
    app: customer-service
spec:
  selector:
    app: customer-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
