apiVersion: v1
kind: Service
metadata:
  name: admin-service-mongodb
  namespace: travel-agency
spec:
  selector:
    app: admin-service-mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
