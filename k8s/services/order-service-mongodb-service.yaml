apiVersion: v1
kind: Service
metadata:
  name: order-service-mongodb
  namespace: travel-agency
spec:
  selector:
    app: order-service-mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
