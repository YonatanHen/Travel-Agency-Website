apiVersion: v1
kind: Service
metadata:
  name: email-service-mongodb
  namespace: travel-agency
spec:
  selector:
    app: email-service-mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
