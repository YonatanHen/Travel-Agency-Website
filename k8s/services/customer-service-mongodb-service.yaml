apiVersion: v1
kind: Service
metadata:
  name: customer-service-mongodb
  namespace: travel-agency
spec:
  selector:
    app: customer-service-mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
