apiVersion: v1
kind: Service
metadata:
  name: user-service-mongodb
  namespace: travel-agency
spec:
  selector:
    app: user-service-mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
