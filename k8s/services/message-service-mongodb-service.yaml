apiVersion: v1
kind: Service
metadata:
  name: message-service-mongodb
  namespace: travel-agency
spec:
  selector:
    app: message-service-mongodb
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None
