apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: travel-agency
  labels:
    app: admin-service
spec:
  selector:
    app: admin-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
